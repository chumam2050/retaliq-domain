version: '3.8'

services:
  domain.retaliq.test:
    build: .
    image: choerul/retaliq-domain
    volumes:
      - .:/workspace:rw
    working_dir: /workspace
    tty: true
    stdin_open: true
    command: bash
